#include "HGuid.h"

//
//HGUID CreateGUID()
//{
//    HGUID uuid;
//#ifdef _WIN32
//    GUID guid;
//    CoCreateGuid(&guid);
//    memcpy(&uuid, &guid, sizeof(HGUID));
//#else
//    uuid_generate(reinterpret_cast<unsigned char*>(&guid));
//#endif
//    return uuid;
//}
